# BCI-SYS 编程手册

> 编程会话的唯一输入。读完即可动手。
> 完整版背景（含推导、否决理由、文献依据）见 `BACKGROUND-FULL.md`，只在需要重开决策时读。

---

## 1. 这是什么

一位父亲为 10 岁重度 ASD 孩子自研的**神经反馈训练原型系统**。开发者一人，每天 2-3 小时。

**本期目标：证明流程跑得通，不问效果。**
本期被试是开发者自己，不涉及孩子；且先不接硬件，用公开数据回放模拟实时。

---

## 2. 系统在做什么

一次会话六个阶段，都不能跳：

| 阶段 | 做什么 |
|---|---|
| S1 | 装配质检（回放期简化为「数据文件可读」） |
| S2 | 参数确认，**并冻结** |
| S3 | 前测 2 min → 算出阈值和 EOG 回归系数 |
| S4 | 训练：6 个 block × 2 min，block 间休息 30 s |
| S5 | 后测 2 min，**与 S3 完全相同** |
| S6 | 收尾，保存，出报告 |

S4 的闭环：

```
读一段(200ms) → 质量判定 → 算 theta → 与阈值比 → 给反馈 → 记录
      ↑                                                    │
      └────────────────────────────────────────────────────┘
```

---

## 3. 关键取值

| 项 | 值 |
|---|---|
| 靶点 | theta 4-8 Hz @ Fz，**方向：上调** |
| 段长 | 200 ms（250 Hz × 0.2 s = 50 采样点） |
| 任务 | n-back，固定 2-back，纯文字 + 键盘 |
| 反馈 | 视觉进度条 |
| 时长 | S3 = 2 min／S4 = 2 min × 6／S5 = 2 min／**会话硬上限 25 min** |
| 阈值目标 | theta 达标时间占比 **75-85%**，每 block 更新 |
| 离线数据 | `E:\workbuddy\2026-09-07-20-50-24\ds007169` |

### 数据实测情况（已核实）

- 18 名被试，250 Hz，linked-ear 参考 —— 与设计一致，**无需重采样、无需重参考**
- 19 导：Fz/Cz/Pz/F3/F4/C3/C4/P3/P4/T3/T4/T5/T6/F7/F8/O1/O2/FP1/FP2
- **无 FCz** → 本期靶点只用 Fz 单通道，硬件到货后再加 FCz
- **无专用 EOG 通道** → 用 FP1/FP2 作代理（channels.tsv 中的实际通道名，大小写以此为准）
- 每人约 481 个 n-back 试次，1-4 级均衡
- `events.tsv` 有 `nback_level` 字段，可用于负荷分组
- `dropped_samples` 事件数差异大：先用 001/002/006/014/015/018/019，sub-012 留作极端测试

---

## 4. 七条硬约束（违反即返工）

### C1 方向必须显式写
曾两次把训练方向搞反。方向错了**不报错、不崩溃、数据看着完全正常**。
任何涉及靶点的地方都必须显式写 `direction="up"`，不许有默认值。

### C2 参数冻结后不许改
S3 结束时把参数拍成快照，S6 之前不许动。**运行期不提供任何修改入口。**
要改参数只能中止会话重来，本次数据标记为中止。

### C3 实时不许用未来数据
实时环路只能向前看。**典型犯法方式：零相位双向滤波。**
一旦用了未来数据，等于系统预知未来，反馈就是假的。而且这个错误不报错。
实时代码和离线代码物理隔离，不共用任何处理逻辑。

### C4 伪迹必须在环路内拦
**最经典的翻车方式：被试眨眼 → theta 飙高 → 系统给奖励 → 被试学会了眨眼。**
练三个月，练出来的是眨眼技能，全程系统不报错。

三级处置：
- **L1** 时域峰峰值：200 ms 滑窗内 Fz 峰峰值 > 100 µV → 该段无效
- **L2** EOG 因果回归扣除：`Fz_clean = Fz - b·EOG`，b 在 S3 估出后**冻结**，S4 不再更新
- **L3** EOG 双路硬门控：EOG 自身超阈 → 该段无效。**EOG 是否决项，不是参考项**

### C5 无效数据只标记，不删除
该段无效时：反馈**暂停**（既不给正向也不给负向）、不计入指标、**必须记录「此段无效」及原因**。
中止率和无效率本身就是本期的关键结果。

### C6 反馈用的数和评效果的数必须分开
若同一个数既给反馈又判效果，"指标变好"就有一个无法排除的解释：**被试只是学会了操纵这个数。**
- 反馈指标 = 实时 theta vs S3 基线
- 评估指标 = **行为表现**（正确率、反应时）

### C7 系统必须拦人，不是提示人
开发者、操作者、被试、评估者是同一个人。**提示对同一个人没有约束力。**
必须硬拦的地方：参数未确认不许开始／到时长上限强制结束不提供延长／被试状态未填不予保存。

---

## 5. 数据留痕三铁律

| 编号 | 铁律 |
|---|---|
| D1 | 原始数据只写一次，永不修改、永不覆盖 |
| D2 | 每次会话必须记录代码版本 |
| D3 | 无效会话只标记，不删除 |

每次会话留下六件套：**原始数据／参数快照／事件日志／会话元信息／被试状态／会话报告**。
前五项不可缺失，缺任一件拒绝保存。

---

## 6. 伪反馈（ABABA）

5 次会话：**1/3/5 真反馈，2/4 伪反馈**。伪反馈的进度条由固定种子的伪随机序列驱动，与 theta 无关，奖励密度与真反馈匹配。

**必须盲化**：分配由启动时读预置序列决定，**不进界面、不进日志摘要、不进控制台**。真实分配写独立揭盲文件，5 次全跑完才允许打开。

**种子必须固定并写入参数快照**，否则违反可重算。

**本期不得据此下任何科学结论。** 5 次、单被试，统计上证明不了任何事。

---

## 7. 本期成功判据（A1-A5）

| 编号 | 判据 |
|---|---|
| A1 | 连续 5 次会话全程无中断完成 |
| A2 | 5 次会话数据均可被评估模块读取并出结果 |
| A3 | 同一评估模块能吃公开离线数据并出结果 |
| A4 | 任一会话重跑一次，结果完全一致 |
| A5 | 单次会话在预定时长内结束，无需人为干预 |

**A1-A5 全满足即本期完成。指标有没有变好，本期不问。**

说的时候必须带上「**在回放模式下**」六个字——以下三项回放期永远验不出，要等硬件：
S1 阻抗质检 / 蓝牙丢包断连 / 干电极漂移。

---

## 8. 本期明确不做

疗效判定／靶点优选／**算法调优**／患者交互适配／多被试比较／行为量表／统计显著性检验。

**算法只求「能跑、可换、留痕」，不求最优。**
只有「算不出数」才允许换实现（限时 2 天）；「数不好看」不允许动。

难度自适应（n-back 档位随正确率调整）本期**只留接口，固定 2-back**。

---

## 9. 迭代计划（7 期，5 周）

### 铁律

**每期必须产出一个能启动、能看到输出的程序。**
写了一堆库函数但没有可运行入口 = 本期未完成，即使代码量很大。

**每期收工必须截一张图存进 `evidence/`。不截图 = 本期未验收。**

### 七期

| 期 | 程序 | 一句话 | 成果物 |
|---|---|---|---|
| **IT1** | `replay_monitor` | 读 ds007169，按 1x 节奏把段吐到终端，同时落盘 JSONL | 终端截图（含速率比）+ JSONL 文件 |
| **IT2** | `theta_probe` | 每段算 Fz theta，跑完按 n-back 负荷分组出折线图 | **PNG 折线图** |
| **IT3** | `qc_monitor` | 加 L1/L2/L3 三级 QC，无效段标红 | 终端截图 + 有效率时间轴图 |
| **IT4** | `feedback_loop` | S3 算阈值，S4 六个 block，进度条实时跳 | **GIF 录屏** |
| **IT5** | `nback_task` | 独立的 2-back 界面，能玩（不依赖脑电） | GIF 录屏 + 行为数据文件 |
| **IT6** | `session_run` | IT4+IT5 连起来，跑完整 S1-S6，出六件套 | 会话目录 + 会话报告 |
| **IT7** | `series_run` | 跑 5 次 ABABA，揭盲，勾 A1-A5 | A1-A5 判据表 + 5 次汇总图 |

**排期**：第 1 周 IT1+IT2／第 2 周 IT3／第 3 周 IT4（最难）／第 4 周 IT5+IT6／第 5 周 IT7。
IT5 不依赖前面任何一期，可提前并行。

### 每期三段

1. **定入口** —— 写清本期程序怎么启动、屏幕上该出现什么、怎么算做完。**没写完不许动手。**
2. **编码** —— 写到能跑为止。缺的算法用最省事实现。
3. **验收** —— 亲手跑一遍，逐条核对，截图存档。

时间盒 1 周 ≈ 15 h。**到期即收，砍范围不延期。**
新想法只进 BACKLOG，不改已定的东西。

**每期的启动方式、屏幕输出、验收判据，在该期开工时（第 1 段）现写。**
现在提前写的判据，到那一期大概率要改。

### 唯一现在就定死的判据：IT2

> **1→4-back 的 Fz theta 必须单调上升。**
> 这是本项目唯一的外部真值（n-back 负荷越高、额中线 theta 越强，文献稳定结论）。
> **算不出这条上升曲线，说明 theta 计算或通道映射有错，不许进 IT3。**